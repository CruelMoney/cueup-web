import React from 'react';

import RequestForm from '../src/shared/components/common/RequestForm';

export default {
    title: 'Request form',
};

export const text = () => (
    <div style={{ width: '500px' }}>
        <RequestForm />
    </div>
);
