import React from 'react';

import JoinThousands from 'routes/BecomeDj/components/JoinThousands';

const SocialProof = () => {
    return (
        <JoinThousands
            title={'Find your DJ on Cueup like thousands of other event organizers'}
            to={'/s'}
            label="Find a DJ"
        />
    );
};

export default SocialProof;
